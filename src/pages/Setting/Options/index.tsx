import * as React from 'react';

export default class Options extends React.PureComponent {
    render() {
        return (
            <div className="c-comp-setting-options">
                options
            </div>
        );
    }
}
